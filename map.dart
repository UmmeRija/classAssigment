// Maps:

// Create a Map with keys as strings and values as integers (e.g., a map of student names and their grades) and print all the key-value pairs.

void main() {
  Map studentData = {
    "name": "rija",
    "id": 10021,
    "batch code": "11-A",
    "grades": "A Grade",
  };
  print(studentData["id"]);
  print(studentData["name"]);
  print(studentData["batch code"]);
  print(studentData["grades"]);
}
